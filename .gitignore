# ── Secrets — never commit ──────────────────────────────────────────────────
.credentials.json

# ── Runtime / machine-specific (Claude Code manages these) ─────────────────
history.jsonl
*.jsonl
cache/
backups/
debug/
downloads/
file-history/
paste-cache/
session-env/
sessions/
shell-snapshots/
tasks/
telemetry/
ide/
daemon/
plans/
todos/
statusline-state.json
mcp-needs-auth-cache.json
policy-limits.json
remote-settings.json
.last-cleanup
.last-update-result.json
settings.local.json

# Project session data and auto-memory stay local (this repo is PUBLIC)
projects/

# ── Plugins: marketplaces and caches are re-fetched on install ─────────────
plugins/cache/
plugins/marketplaces/
plugins/data/
plugins/blocklist.json*
plugins/install-counts-cache.json
plugins/known_marketplaces.json
plugins/.last_inuse_sweep

# ── Misc ───────────────────────────────────────────────────────────────────
*.tmp
**/__pycache__/
*.pyc

# Auto-managed by claude plugin
plugins/installed_plugins.json
*.bak
